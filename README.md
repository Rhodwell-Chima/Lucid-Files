# Lucid-Files

**Lucid-Files** is a modular and configurable file organization tool written in Rust. It provides a robust framework for
scanning directories, filtering files based on specific criteria, classifying them into categories, and performing
actions such as moving, copying, or deleting.
